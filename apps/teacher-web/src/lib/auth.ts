// Phase 4: @supabase/ssr browser + server clients (email-OTP session, teacher-lms
// Q4) that mint/refresh the teacher JWT and attach it as Bearer to the B /api
// routes. Placeholder until the auth stack lands.
export {};
